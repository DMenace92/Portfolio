{
  "name": "api",
  "version": "1.0.0",
  "description": "Portfolio API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "concurrently \"nodemon server.js\" \"npm start --prefix Portfolio\"",
    "build:client": "npm run build --prefix Portfolio",
    "seed:admin": "node scripts/seedAdmin.js",
    "heroku-postbuild": "cd Portfolio && npm install --only=dev && npm install && npm run build"
  },
  "author": "Dennis A, Enwiya",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^2.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "node-appwrite": "^29.0.0",
    "nodemailer": "^6.9.3",
    "nodemon": "^2.0.22",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
